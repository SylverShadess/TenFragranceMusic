<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/playlist_item_rll"
    android:layout_marginLeft="5dp"
    android:layout_marginRight="5dp"
    android:layout_marginTop="5dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp"
    android:background="@color/darker_tohka_purple">

    <ImageView
        android:id="@+id/ivplay_img"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:paddingLeft="10dp"
        android:layout_centerVertical="true"
        android:src="@drawable/baseline_album_124"/>

    <TextView
        android:id="@+id/tvplay_name"
        android:layout_width="240dp"
        android:layout_height="30dp"
        android:layout_toRightOf="@+id/ivplay_img"
        android:layout_marginLeft="20dp"
        android:text="Song Name"
        android:maxLines="1"
        android:gravity="center_vertical"
        android:ellipsize="end"
        android:padding="4dp"
        android:textSize="18sp"/>

    <TextView
        android:id="@+id/tvplay_artist"
        android:layout_width="240dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tvplay_name"
        android:layout_alignLeft="@+id/tvplay_name"
        android:layout_marginTop="5dp"
        android:text="Artist"
        android:textSize="15sp"/>

    <ImageView
        android:id="@+id/iv_delsong"
        android:layout_width="35dp"
        android:layout_height="35dp"
        android:layout_alignParentRight="true"
        android:layout_marginRight="20dp"
        android:layout_centerVertical="true"
        android:src="@drawable/baseline_playlist_remove_124"
        app:tint="@color/semitrans_white"/>

</RelativeLayout>